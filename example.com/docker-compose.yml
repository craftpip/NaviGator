services:
  web:
    image: node:22-alpine
    container_name: example-com
    working_dir: /www
    command: ["node", "server.js"]
    ports:
      - "8080:8080"
    volumes:
      - .:/www
    restart: unless-stopped
