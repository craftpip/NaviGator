services:
  web:
    image: busybox:latest
    container_name: example-com
    command: ["httpd", "-f", "-p", "8080", "-h", "/www"]
    ports:
      - "8080:8080"
    volumes:
      - .:/www:ro
    restart: unless-stopped
